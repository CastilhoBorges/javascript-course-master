// Math operators 
let actualYear = 2024; 
const ageCastilho = actualYear - 2005;
const ageCintia = actualYear - 1978;
console.log(ageCastilho, ageCintia);

const firstName = 'Augusto';
const lastName = 'Castilho';
console.log(firstName + ' ' + lastName);

// Assigment operators (Operadores de atribuição)
let x = 10 + 5; 
x += 10; // x = x + 10 = 25
x *= 4; // x = x * 4 = 100
x++; // x = x + 1 

// Comparison operators 
console.log(ageCastilho > ageCintia); // False, because age to Castilho do not great that to Cintia. 
/*
Others comparison operators 
<, >=, <=, === 
*/
